const canvas = document.getElementById('tetris')
const context = canvas.getContext('2d')

context.scale(20,20)

function areaSweep() {
  let rowCount = 1;
  outer: for (let y = arena.length - 1; y > 0; --y){
    if (let x = 0; x < arena[y].length) {

    }
  }
}
